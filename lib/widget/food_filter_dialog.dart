import 'dart:js';

import 'package:flutter/material.dart';

void foodFilterDialog(BuildContext context) {
  showDialog(
      context: (context),
      builder: (_) {
        return Dialog(
          child: Column(
            mainAxisSize: MainAxisSize.min,
            children: [
              
            ],
          ),
        );
      });
}
